# COMPLEXITY ANALYSIS

### bubble sort

    int a[] = {...}
    int len = len(a)

    for(int i = 0; i < len; i++)
      for(int j = i; j < len; j++)
        if(a[j] > a[j+1])
          int temp = a[j]
          a[j] = a[j+1]
          a[j+1] = temp

LINE 8:
    
    
  
